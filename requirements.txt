# ESCROW: what the code needs to run.
#
# The engine (code/escrow/) and the gates (code/tests/) need NOTHING but the Python 3
# standard library. Nothing in this file is required to reproduce a unit test, and nothing
# here is required to run the mechanism experiments e7, e8, e9, e13, e5 or the Wikipedia demo.
#
# numpy is OPTIONAL everywhere. The engine uses it only as a vector fast path and falls back
# to pure Python without it; the two paths are asserted equal by
# code/tests/test_batch.py::test_numpy_path_equals_pure_path.
#
# Nothing is pinned. A minimum version is given only where the code needs one.
# Data files are not installed by pip: see DATA.md for every external input.

# --- optional everywhere: the engine's vector fast path ---------------------
# also used directly by e4_baseline_army.py, e10_reverb45k.py, mb20k_benchmark.py,
# llm_graph_formation.py
numpy

# --- clustering baselines and the ARI metric --------------------------------
# e4_baseline_army.py, e10_reverb45k.py, mb20k_benchmark.py: KMeans, HDBSCAN,
#   AgglomerativeClustering, adjusted_rand_score, silhouette_score
# lazada_c2_rawkey.py, llm_graph_formation.py: adjusted_rand_score
# >=1.3 because sklearn.cluster.HDBSCAN first ships in scikit-learn 1.3
scikit-learn>=1.3

# --- mb20k_benchmark.py only ------------------------------------------------
# scipy.cluster.hierarchy.linkage/fcluster and scipy.spatial.distance.squareform
# for the agglomerative baseline on the precomputed cosine distance matrix
scipy

# --- the frozen sentence embedding every baseline receives ------------------
# e4_baseline_army.py, e10_reverb45k.py, mb20k_benchmark.py, llm_graph_formation.py
# model: sentence-transformers/all-MiniLM-L6-v2, downloaded on first use
sentence-transformers

# --- the language-model comparison ------------------------------------------
# llm_graph_formation.py: Qwen2.5-14B-Instruct in bf16 on one GPU
transformers
# torch is also mb20k_benchmark.py's GPU k-means at K=10,000, and a dependency of
# sentence-transformers and transformers
torch

# --- e8_tree_arms.py only ---------------------------------------------------
# river.tree.HoeffdingTreeClassifier and ExtremelyFastDecisionTreeClassifier,
# the VFDT and EFDT arms of the false-mint null
river

# --- the figures ------------------------------------------------------------
# make_figures.py, fig_baselines.py, fig_cost.py, fig_llm.py, fig_abstain.py
matplotlib
